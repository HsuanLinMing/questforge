name: questforge_ui_contract
description: QuestForge UI contract (pure Dart). Engine -> UI command payload models.
version: 0.1.0
homepage: https://example.invalid

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies: {}

dev_dependencies:
  lints: ^3.0.0
